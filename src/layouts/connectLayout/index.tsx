import React, { FC } from 'react';
import { StyledConnectLayout } from './style';
const ConnectLayout: FC = ({ children }) => {
  return <StyledConnectLayout>{children}</StyledConnectLayout>;
};
export default ConnectLayout;
